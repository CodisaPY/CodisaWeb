export type SalaReunion = {
  idSala: number;
  nombre: string;
  color: string;
  estado: string;
  createdAt: string;
};

export type SalaReunionInput = {
  nombre: string;
  color: string;
  estado: string;
};
